# init.d (service) vs. systemd (systemctl) vs. upstart (initctl)

# services to be loaded initially are located at file /etc/init.d or /etc/initab
# These service startup files are located at /etc/init
